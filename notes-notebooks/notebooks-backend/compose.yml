name: notebooks-backend

services:
  notebooks:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3333:3333
    environment:
      - PORT=3333
    develop:
      watch:
        - action: sync
          path: ./src
          target: /app/src
